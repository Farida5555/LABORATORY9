name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check files
        run: ls -la

      - name: Create Artifact (Zip)
        run: |
          zip -r project-v1.zip .

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: project-v1.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}